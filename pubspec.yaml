name: centinela_milagro
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.10.3

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.2.1
  go_router: ^13.2.0
  flutter_riverpod: ^3.0.0
  flutter_animate: ^4.5.0
  flutter_map: ^7.0.2
  flutter_map_tile_caching: ^9.1.0
  latlong2: ^0.9.1
  uuid: ^4.4.0
  intl: ^0.19.0
  flutter_compass: ^0.8.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
